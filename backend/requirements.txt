# Universal Accounting Platform — backend dependencies
# Session 1. Install with:  pip install -r requirements.txt  (inside the venv)

# Web framework / server
fastapi>=0.115
uvicorn[standard]>=0.30

# Database
sqlalchemy>=2.0
psycopg2-binary>=2.9
alembic>=1.13

# Config / env
python-dotenv>=1.0
pydantic>=2.7
pydantic-settings>=2.3

# Auth (Session 3)
passlib[bcrypt]>=1.7
python-jose[cryptography]>=3.3
python-multipart>=0.0.9

# Testing
pytest>=8.0
httpx>=0.27
